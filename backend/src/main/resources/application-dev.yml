spring:
  jpa:
    show-sql: ${JPA_SHOW_SQL:false}

ltss:
  security:
    cors:
      allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}
    refresh-cookie:
      secure: ${REFRESH_COOKIE_SECURE:false}

logging:
  level:
    com.ltss: ${LTSS_LOG_LEVEL:DEBUG}
